port: 7880
node_ip: "{{LIVEKIT_NODE_IP}}"
bind_addresses:
  - ""

rtc:
  tcp_port: 7881
  udp_port: 7882
  use_external_ip: false

room:
  auto_create: false

keys:
  "{{LIVEKIT_API_KEY}}": "{{LIVEKIT_API_SECRET}}"
